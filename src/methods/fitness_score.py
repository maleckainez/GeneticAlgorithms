from methods.utils import create_population
def calc_fitness_score(ITEMS):
    # TODO: We have to implement new way to deal with calculating the fitness Score.
    #  NOTE: Previous one took whole population matrix instead of the stream. It overflow the
    #  memory and triggered numpy safeguards.
    return 0
